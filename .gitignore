# Personal sensor-data exports (real trip recordings, multi-MB) — never publish
Test data.json
victoria-training-*.json

# Local tool state
.claude/

# Local test harness pages (headless-browser verification, not part of the app)
__*.html
